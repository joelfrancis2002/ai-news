# @ai-newsroom/ai

**Phase 8+**: claim extraction, evidence evaluation, and summarization with Zod-validated JSON. Configure with `AI_PROVIDER` and provider API keys in `.env` (never expose keys to the frontend).
